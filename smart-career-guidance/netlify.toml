[build]
  publish = "public"
  functions = "functions"
  command = "npm run build" # We will set this up in a package.json

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
